# ─────────────────────────────────────────────────────────────────────────────
# InvestTrack — Nginx site configuration
#
# HTTP (порт 80) — для локальной разработки и перенаправления на HTTPS.
# HTTPS (порт 443) — раскомментируйте блок ниже и настройте SSL-сертификаты.
# ─────────────────────────────────────────────────────────────────────────────

upstream flask_app {
    server app:5000;
    keepalive 32;
}

# ── HTTP ─────────────────────────────────────────────────────────────────────
server {
    listen 80;
    listen [::]:80;
    server_name _;

    # Uncomment to redirect all HTTP → HTTPS (when SSL is configured):
    # return 301 https://$host$request_uri;

    # ── Статические файлы — отдаёт Nginx напрямую (минуя Gunicorn) ──────────
    location /static/ {
        alias /app/static/;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable";
        add_header X-Content-Type-Options nosniff;
        access_log off;
        gzip_static on;
    }

    # ── Health check — без rate limit и access лога ──────────────────────────
    location = /health {
        proxy_pass http://flask_app;
        access_log off;
        proxy_read_timeout 5s;
    }

    # ── Login — жёсткий rate limit ───────────────────────────────────────────
    location = /api/auth/login {
        limit_req zone=login burst=3 nodelay;
        limit_req_status 429;
        proxy_pass http://flask_app;
    }

    # ── Telegram webhook — без rate limit (Telegram IP-адреса) ──────────────
    location /api/telegram/ {
        proxy_pass http://flask_app;
    }

    # ── API — умеренный rate limit ───────────────────────────────────────────
    location /api/ {
        limit_req zone=api burst=20 nodelay;
        limit_req_status 429;
        proxy_pass http://flask_app;
    }

    # ── Всё остальное (страницы) ─────────────────────────────────────────────
    location / {
        limit_req zone=general burst=30 nodelay;
        proxy_pass http://flask_app;
    }
}


# ── HTTPS (раскомментировать после настройки Let's Encrypt) ──────────────────
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name yourdomain.com;
#
#     # Сертификаты (certbot кладёт их в /etc/letsencrypt/live/)
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     # Современные настройки TLS (Mozilla SSL Config Generator — Intermediate)
#     ssl_protocols             TLSv1.2 TLSv1.3;
#     ssl_ciphers               ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache         shared:SSL:10m;
#     ssl_session_timeout       1d;
#     ssl_session_tickets       off;
#     ssl_stapling              on;
#     ssl_stapling_verify       on;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # Те же location-блоки что и в HTTP-сервере выше:
#     location /static/ { ... }
#     location = /health { ... }
#     location = /api/auth/login { ... }
#     location /api/ { ... }
#     location / { ... }
# }
